# 熊出没集团 — Phase 3 规划

## 目标

Phase 3 将熊出没集团从"纯 TRAE 平台"升级为"多引擎 + IM 集成 + 可视化"的完整系统。

## 三大方向

### 1. DeepSeek Harness 接入

**目的**：扩展运行环境，支持外部模型调度，突破 TRAE 单引擎限制。

**实现内容**：
- 配置文件：DeepSeek API 接入参数（endpoint/key/model）
- 接口定义：harness 接口（Leader 通过 harness 调度 Worker）
- 路由策略：TRAE Agent vs DeepSeek API 的任务路由规则
- 降级方案：DeepSeek 不可用时回退到纯 TRAE

**验收标准**：Leader 能通过 harness 接口派发任务给 DeepSeek，结果能正确返回并进入验证流程。

### 2. 飞书 IM 集成

**目的**：用户通过飞书给熊大派任务，熊大秒回确认，完成后飞书通知。

**实现内容**：
- 秒回机制：用户发消息 → 熊大立即回复"收到，正在处理"
- 任务派发：解析飞书消息为结构化任务
- 进度通知：每个波次完成后推送进度
- 交付通知：最终交付物通过飞书发送

**验收标准**：用户通过飞书发一条任务消息，熊大秒回确认，完成后飞书推送交付通知。

### 3. 纸片人工作模式

**目的**：14 个角色用 2D 卡通形象可视化展示工作状态。

**实现内容**：
- 可视化看板 HTML：14 个角色卡片（使用已有角色图片）
- 工作状态实时展示：空闲/执行中/等待验证/完成
- 任务流程可视化：当前波次、并行状态、进度条
- 交互面板：可点击角色查看详情

**验收标准**：打开看板能看到 14 个角色的实时工作状态，任务执行过程中状态自动更新。

## 依赖关系

```
批次1（DeepSeek harness）── 无依赖，可立即开始
批次2（飞书 IM 集成）── 无依赖，可与批次1并行
批次3（纸片人工作模式）── 无依赖，可与批次1/2并行
批次4（集成验证）── 依赖批次1-3
```

## 实施顺序

1. **批次1 + 批次2 + 批次3** 并行推进
2. **批次4** 收尾验证
