# 包含真实密钥的配置文件 — 仅本地使用，不推送到 GitHub
config/harness-config.json
config/lark-config.json

# 会话记忆（临时数据）
memory/session/

# 系统文件
*.tmp
*.bak
.DS_Store
Thumbs.db

# IDE 生成目录
.trae-html-share-packages/

# 构建产物（含 base64 内嵌图片，体积大，可由 build-standalone.js 重新生成）
dashboard-standalone.html

# 废弃的旧图片（已被 3D 版本替代）
assets/characters/char-*.jpg
assets/sprites/
