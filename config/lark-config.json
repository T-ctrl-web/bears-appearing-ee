{
  "version": "3.0.0",
  "description": "飞书 IM 集成配置 — Phase 3 秒回+通知",

  "app_config": {
    "app_id": "YOUR_LARK_APP_ID",
    "app_secret": "YOUR_LARK_APP_SECRET",
    "verification_token": "YOUR_VERIFICATION_TOKEN",
    "encrypt_key": "YOUR_ENCRYPT_KEY"
  },

  "features": {
    "instant_reply": {
      "enabled": true,
      "description": "用户发消息后熊大秒回确认",
      "template": "收到！我是熊大（总裁）。正在评估任务复杂度，稍后给你拆解结果。",
      "max_delay_ms": 3000
    },
    "task_dispatch": {
      "enabled": true,
      "description": "解析飞书消息为结构化任务",
      "parse_rules": [
        "如果消息包含明确的任务描述，直接作为任务",
        "如果消息是模糊的需求，追问澄清",
        "如果消息包含附件，解析附件内容"
      ]
    },
    "progress_notify": {
      "enabled": true,
      "description": "每个波次完成后推送进度",
      "template": "波次{wave}完成！{completed_steps}/{total_steps}步骤已执行。\n{completed_summary}",
      "on_error": true,
      "on_reject": true
    },
    "delivery_notify": {
      "enabled": true,
      "description": "最终交付通过飞书发送",
      "template": "任务完成！交付物：\n{delivery_list}\n\n查看详情：{file_links}",
      "include_summary": true
    }
  },

  "message_types": {
    "text": "纯文本消息，直接解析为任务",
    "rich_text": "富文本消息，提取正文内容",
    "file": "文件消息，下载并解析",
    "image": "图片消息，识别内容"
  },

  "reply_format": {
    "type": "interactive_card",
    "card_template": {
      "header": "熊出没集团任务通知",
      "elements": ["正文内容", "进度条", "按钮组"]
    }
  }
}
